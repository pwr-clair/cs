# 인수인계 — PWR CS 프로젝트 현황 (2026-07-08 세션)

다른 세션/담당자에게 넘기는 현황 스냅샷. 커밋 SHA·라이브 상태·클라라 대기 액션 포함.

## 레포/도메인 구조
- **`pwr-clair/cs`** (관리자용): 승인앱(루트 `index.html`) + GAS 엔진(`gas/Code.gs`) + 리포트(`docs/reports/`). Pages: `pwr-clair.github.io/cs/`.
- **`pwr-clair/guide`** (게스트용, 신설): 게스트 가이드 단독. 커스텀 도메인 **https://pwr-guide.online** (HTTPS 강제·DNS 전파 완료). 구 `cs/guide/`는 이 도메인으로 **리다이렉트 셔틀**만 남음(원본 이미지 보존).

---

## 1) 게스트 가이드 (pwr-guide.online) — 라이브
단일 HTML, **4개 언어**(EN 기본 / KO / JA / ZH), 상단 우측 **국기 원형 스위처**(선택 localStorage 기억). 이미지·구조 1벌 공유, 텍스트만 `data-i18n` 사전 스왑.
- 섹션: Welcome → 무료셔틀(T1/T2 통일 스텝·색상 바·깔끔한 04번 시간표) → 택시 카드(한국어 주소 고정) → 건물찾기 → 숙박(체크인 초록/아웃 빨강) → 주변안내(14곳) → Support&Contact.
- **최신 변경(`guide@5d542fc`)**: 주변안내 14곳의 **구글맵 링크·"지도↗" 버튼 전부 제거** → 클릭 없는 정보 카드(가게명+설명). 라이브 반영 완료.
- WhatsApp 라벨 4개 언어 "열기/바로가기" 통일. 택시 카드의 한국어 주소·하차문구는 모든 언어에서 한국어 고정(기사용).
- ⚠️ **KO/JA/ZH 번역은 클로드 초안** — 네이티브/클라라 검수 대상(요금·규칙·주소·이웃 설명 포함). 수정은 JS 사전(`I18N`)만 고치면 됨.
- 커밋: `2a0eb01`(init+도메인) → `5d542fc`(맵링크 제거). 배포 = GitHub Actions, 이후 push 자동.
- 가이드 v1.1~v2.2 상세 이력: `docs/reports/2026-07-08-guide-v2.md`.

## 2) CS DESK 승인앱 (`cs@01ce742`) — 라이브
백로그 85건 대응:
- **"지난 문의" 원탭 dismiss**(status=dismissed, 데이터 보존·발송/학습 제외) + **일괄 dismiss**(오늘 0시 KST 이전 pending, confirm 1회).
- 대기·보냄 목록 **게스트 수신시각 최신순** 정렬, 카드에 "M/D HH:MM 도착" 표시.
- 순수 로직 16/0 PASS. GAS는 신규 초안에 `receivedAt` 적재 + 기존분 백필 함수(`backfillDraftReceivedAt`) 추가.

## 3) GAS 엔진 (`cs@116caf6`) — ⚠️ 클라라 재붙여넣기 대기
Code.gs에 4건 반영(아직 **PWR-CS-Engine에 미적용**):
- **A. urlfetch 쿨다운 가드**: 소진 감지 시 60분 쿨다운(`CS_URLFETCH_COOLDOWN_UNTIL`), 자동 run skip(Gmail 미접촉). 자동 적용.
- **B. 백로그 컷오프**: `CS_CUTOFF`(ISO) 이전 스레드는 초안 없이 라벨만 이동. 속성 설정 시 작동.
- **C. 가이드 팩트 프롬프트 탑재**: CLARA_SYSTEM에 셔틀·요금·룰·문의 등 확정 팩트 + 가이드 링크 권장. 자동 적용.
- **D. 백로그 질문→CS-DB 시트 적재**: `exportBacklogQuestionsToSheet()`(수동) + import의 빈답변-스킵 수정(fill-later 흡수 함정 해소).
- 순수 로직 39 PASS. 상세: `docs/reports/2026-07-08-quota-and-knowledge.md`.

---

## 🔴 클라라 대기 액션 (우선순위)
1. **GAS Code.gs 재붙여넣기 1회** (A·B·C·D 전부) → A·C 자동 적용됨.
2. **(선택) B 컷오프**: Script Property `CS_CUTOFF` = 날짜(예 `2026-07-08`) 설정 → 과거 백로그 스레드 초안 없이 정리. (KST 정밀컷은 `...T00:00:00+09:00`)
3. **D 코퍼스 재료화**: `exportBacklogQuestionsToSheet()` 실행 → CS-DB 시트 말단 질문 적재 → **clara_reply(C열)만 채움** → `importCorpusFromSheet()` 실행(빈 행 스킵, 채운 것만 흡수). 나눠 여러 번 재실행 안전.
4. **가이드 다국어 검수**: pwr-guide.online 폰에서 KO/JA/ZH 확인(특히 요금·규칙·이웃 설명).

## 미결/참고
- 이미지 원본 대용량(정문/로비/엘베 5–7MB, lazy만 적용) — 재인코딩 미적용(이미지 수정 금지 지침).
- 가이드 새 도메인은 `noindex` 아님(검색 노출). 비공개 원하면 요청.
- 순수 로직 테스트는 스크래치패드 실행(레포 미커밋) — `tests/`로 커밋 원하면 요청.
- 프로젝트 규칙: 설계/팩트 변경은 임의 결정 금지 → Fable 회송. 팩트 창작 금지.

## 커밋 요약(이번 세션 관련)
| 레포 | SHA | 내용 |
|---|---|---|
| cs | `116caf6` | quota guards(A·B) + guide facts(C) + backlog questions to sheet(D) |
| cs | `a667cc6` | 구 guide 경로 → pwr-guide.online 리다이렉트 |
| cs | `01ce742` | dismiss(단건+일괄) + 대기 수신시각 정렬 |
| guide | `5d542fc` | 주변안내 구글맵 링크 제거 |
| guide | `2a0eb01` | 게스트 가이드 단독 분리 + 커스텀 도메인 |
