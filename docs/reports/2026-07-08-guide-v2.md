# 게스트 가이드 v1.2 → v2.2 — 변경 이력 (인수인계용)

- 기간: 2026-07-07 ~ 2026-07-08
- 대상 파일: `guide/index.html` (단일 self-contained HTML, 다국어)
- 레포: `pwr-clair/cs` · 라이브: https://pwr-clair.github.io/cs/guide/
- 배포: GitHub Actions (`.github/workflows/pages.yml`, paths에 `guide/**`). 커밋마다 push→Actions success→deployment sha==HEAD→라이브 검증.
- 검증 방식: 모바일 375–380px 프리뷰 스크린샷 + 콘솔 에러 0 + 라이브 콘텐츠 grep.

## 커밋 체인
| 커밋 | 버전 | 내용 |
|---|---|---|
| `5a7f1c2` | v1.2 | 8건 (아래) |
| `7ecc939` | v1.3 | 4건 (아래) |
| `9bd7de8` | v1.4 | 하단 섹션 통합·재정렬 |
| `6867b03` | v2 | 다국어 EN/KO/JA/ZH |
| `1b3e0af` | v2.1 | KO WhatsApp 라벨 |
| `d9b68a8` | v2.2 | WhatsApp 라벨 4개 언어 통일 + Around 섹션 전면 교체 |

## v1.2 (`5a7f1c2`) — 8건
1. 히어로 sub("A self check-in~") 좌우 폭 통일(34ch 캡 제거)
2. 하단 Support&Contact 블록 맨 아래로, 기존 Contact는 OTA 한 줄만
3. FROM T1/T2 타이틀 "Terminal N → PWR" + 파란 배경 바
4. TO AIRPORT "PWR → Terminal (T1/T2)" + 빨간 배경 바
5. 공항행 정류장 캡션에 "오른쪽(Int'l Business Center) 시간표 하단" 안내
6. 시간표 사진 → 깔끔한 HTML 표(실제 이미지 데이터 05–12시 전사, "hour/minute" 단어 제거)
7. By Taxi 주소 텍스트 −3pt
8. Notice: 하우스룰(빨강)·초록 안내문을 luggage/parking/trash와 동일 행 레이아웃으로 통일, 색상 유지, 순서 지정(luggage→parking→non-smoking→max2→no pets→no parties→trash→do not open)

## v1.3 (`7ecc939`) — 4건
1. 동영상 제거(Shorts R2jEc-ogdWU 임베드 삭제, .video-slot CSS·복원 주석만 잔존)
2. By Taxi 주소 +1.5pt (현재 17px)
3. Check-in=초록 / Check-out=빨강 배경(Notice와 동일 스타일)
4. OTA 줄을 Support&Contact 아래로

## v1.4 (`9bd7de8`) — 하단 통합
- 따로 놀던 "Support&Contact 카드"와 "Contact 섹션"을 하나의 섹션으로 병합.
- 최종 순서: 섹션헤더 **SUPPORT & CONTACT** → We're here to help → Hours → WhatsApp → LINE → WeChat → 지연 안내 → (구분선) → booking platform 회신 줄.

## v2 (`6867b03`) — 다국어
- HTML 1개에 EN/KO/JA/ZH 4개 언어. 이미지·레이아웃 1벌 공유, 텍스트만 `data-i18n` 키 사전 스왑. 영어=DOM 원문, KO/JA/ZH=JS 사전.
- 상단 스티키 스위처(EN/한국어/日本語/中文), 선택 즉시 전환 + localStorage 기억. 기본 English.
- 택시 카드의 한국어 주소·하차 문구는 4개 언어 전부 한국어 고정(기사님이 읽는 부분). 고유명사·요금(₩)·시간·시간표 숫자 원문 유지, 상점명만 현지화.
- CJK 폰트 폴백 추가. 면책 문구 없음(4개 언어 동등, 클라라 결정).

## v2.1 (`1b3e0af`)
- KO WhatsApp 라벨 "WhatsApp으로 연락" → "WhatsApp 바로가기".

## v2.2 (`d9b68a8`)
1. WhatsApp 버튼 라벨 4개 언어 통일: EN `Open WhatsApp` / KO `WhatsApp 바로가기` / JA `WhatsAppを開く` / ZH `打开WhatsApp`.
2. Around(이웃) 섹션 전면 교체 — 클라라 제공 확정 데이터 그대로. 4카테고리·14장소:
   - 🍽️ 식당/바(7): 공항밥집, 신설직화화로구이, 전주현대옥, 고향집, BHC치킨 인천공항점, The Cinder Bar (Incheon Airport), The Jet Lagged Lizard
   - ☕ 카페(2, 체크인 전 대기 안내 포함): Han Coffee Shop, 메가커피 영종합동청사점
   - 💊 약국 & 의원(2): 연세약국 (Yonsei Pharmacy), 연세공항의원
   - 🏪 편의점(3): emart24, CU, 7-Eleven
   - UI: 각 장소 = 탭하면 구글맵 새 탭으로 열림, 우측 "지도/Map ↗" 표시. URL 미노출.
   - 다국어: 가게명 고유명사 전 언어 공통, 설명·카테고리·"지도" 라벨만 언어별. i18n 키 104/104 커버리지.

## ⚠️ 열린 항목 (다음 담당자 유의)
- **KO/JA/ZH 번역은 전부 클로드 초안** — 요금·규칙·주소·이웃 설명 포함. 클라라/네이티브 검수 전까지 "초안 라이브". 수정은 JS 사전만 고치면 됨.
- **v2.2에서 삭제된 팩트**: 기존 "장보기 배달 — Lotte Mart Unseo 온라인", "마마님(한식)"이 새 목록에 없어 빠짐('교체' 지시대로). 재추가 여부 클라라 확인 필요.
- 이미지 원본 대용량(정문/로비/엘베 5–7MB) — loading="lazy"로만 완화, 재인코딩 미적용(이미지 수정 금지 지침).
- 동영상(Shorts R2jEc-ogdWU) 제거 상태, `.video-slot` CSS·복원 주석 잔존해 재삽입 용이.
- 프로젝트 규칙: 설계/팩트 변경은 임의 결정 금지, Fable(챗) 회송. 팩트 창작 금지.
