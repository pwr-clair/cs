# 완료 보고 — M2a (코퍼스 + 초안 생성 + 텔레그램 푸시)

> 2026-07-06 / 클코 / 선행 확인 게이트 통과(Fable 실측: pendingBookings.channelBookingId 존재) 후 착수.
> 대상: `gas/Code.gs` (M1 파일에 M2a 섹션 추가, 단일 파일 유지 — Clara 붙여넣기 1회).

## 수정 지점 (before → after)
| 지점 | 내용 |
|---|---|
| `pollCsInbox()` | 수신 루프 끝에 `processInboxToDrafts()` 호출 추가(try/catch — API 실패해도 적재는 유지). `if(!threads.length) return` 제거해 신규 메일 없어도 초안 파이프라인은 돈다 |
| `processInboxToDrafts()` (신규) | cs/inbox 신규건(파싱성공·초안없음) → Claude 초안 생성. 폴링 1회당 `DRAFT_BATCH=5` 캡 |
| `makeDraftFor_/claudeDraft_/extractJson_` (신규) | Claude Messages API(`/v1/messages`, haiku `claude-haiku-4-5-20251001`, max_tokens 1024) 호출 → `cs/drafts/{msgId}{reply,replyKo,category,confidence,status:'pending',editedReply,lang,guest,bookingId,replyTo,sirvoyId,room,model,createdAt}` |
| `retrieveExamples_` (신규) | corpus few-shot 검색: 같은 언어 +5점 + 키워드 겹침 점수 상위 5건 |
| `findSirvoy_/loadPending_` (신규) | 매핑 `cs/inbox.bookingId ↔ pendingBookings.channelBookingId` 직결. 매칭 실패 시 null(폴백 미구현 — Fable 지시) |
| `tgNotify_` (신규) | 텔레그램 푸시 `[PWR CS] {게스트} ({방|미상}) {원문 첫줄}… → 초안 대기` |
| `importCorpusFromSheet()` (신규·1회성) | CS-DB 시트 → `cs/corpus{상황요약,최종답변,lang,origin:'구축',src:'sheet'}`. 헤더 키워드 매핑 |
| `mineCorpusFromGmail()`/`stripQuoted_` (신규·1회성) | `from:guest.booking.com` 스레드에서 (게스트 메시지 ↔ 클라라 회신) 쌍 추출 → corpus. 회신 없는 스레드 스킵. origin:'구축', src:'gmail' |

- 시크릿: `ANTHROPIC_KEY`, `TG_TOKEN`, `TG_CHAT` 모두 스크립트 속성에서만 read. 코드/레포에 없음.
- 번역 면책: 게스트 언어가 영어가 아니면 **모델이 그 언어로 reply 끝에 한 줄 첨부**하도록 시스템 프롬프트에 지시(하드코딩 대신 — 'eu' 등 다국어에서 정확).

## 검증 결과
- ✅ **순수 로직 로컬 검증(JavaScriptCore)**: `extractJson_`(프로즈+JSON 래핑), `stripQuoted_`(##- 마커/`>` 인용 절단), `findSirvoy_`(매칭/무매칭 null), `retrieveExamples_`(언어 가중+키워드) — 전 항목 PASS.
- ✅ 중괄호 균형 129/129, top-level 함수 28개.
- ⚠️ **엔드투엔드(Claude 호출·Firebase 쓰기·텔레그램·시트·Gmail 마이닝)는 GAS에서만 검증 가능** → Clara 실행 필요.

## 미확정/가정 (Fable 확인 요청)
1. **pendingBookings 실경로**: `pendingBookings` → `app/pendingBookings` 순으로 시도. 둘 다 아니면 실경로 회신 요망.
2. **room 필드명**: `room|roomName|unit|roomType` 순 시도 → 없으면 푸시에 "미상". 실제 방 필드명 알려주면 정확 표기.
3. **CS-DB 시트 열**: 헤더에 상황/요약/질문/문의 · 답변/응대/회신 · 언어/lang 키워드로 매핑. 매핑 실패 시 로그에 헤더 출력 → 열 이름 회신 요망.
4. 유사사례 검색은 임베딩 아님(키워드 겹침) — CJK는 토큰 겹침이 약해 언어 매칭 위주. 정교화는 후속.
5. category는 자유 분류(한국어 짧은 라벨), confidence는 저장만·게이팅 미사용(설계 준수).

## Clara 준비물 체크리스트 (M2a 실행)
0. 레포의 새 `gas/Code.gs` 전체를 GAS(PWR-CS-Engine)에 덮어붙이기.
1. **ANTHROPIC_KEY**: console.anthropic.com → API Keys 발급 → 스크립트 속성 `ANTHROPIC_KEY` 등록.
2. **텔레그램**: ①텔레그램에서 **@BotFather** → `/newbot` → 이름·username → **봇 토큰** → 스크립트 속성 `TG_TOKEN`. ②그 봇에게 아무 메시지 1회 전송 후 `https://api.telegram.org/bot<토큰>/getUpdates` 열어 `chat.id` 확인 → 스크립트 속성 `TG_CHAT`.
3. **코퍼스 1회성 함수 2개 실행** (트리거 아님, 에디터에서 직접): `importCorpusFromSheet()` → 실행 로그 확인, `mineCorpusFromGmail()` → 스레드/적재 건수 로그 확인.
4. (선택) 초안 파이프라인은 기존 `pollCsInbox` 1분 트리거에 이미 연결됨 — 별도 트리거 불필요.

## 검증 시나리오 (Clara)
- 코퍼스 2함수 실행 후, 신규 부킹 메일 1건 도착 → `cs/drafts/{msgId}` 에 reply/replyKo/category/confidence 생성 + 텔레그램 푸시 1건 수신되는지.
- room이 "미상"으로 뜨면 위 가정 2(room 필드명) 회신.

## 배포 결과
- 커밋/푸시 SHA + Pages 배포 결과는 본 커밋 직후 기록(아래 갱신).
